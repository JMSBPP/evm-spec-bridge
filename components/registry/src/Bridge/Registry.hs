-- | Method registry — the schema source of truth for codegen
--
-- Phase 1 placeholder. This module exists so the component is real — a package
-- with no modules is not evidence that the seam works.
module Bridge.Registry (componentName) where

componentName :: String
componentName = "evm-spec-bridge-registry"
