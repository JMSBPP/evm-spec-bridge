---
phase: 04-json-rpc-service-surface-and-fault-taxonomy
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/registry/src/Bridge/Registry.hs
  - components/registry/test/Main.hs
  - solidity/test/SpecFixtures.t.sol
  - scripts/fixtures-test.sh
  - justfile
autonomous: false
parallelization: true
requirements: [SRV-07]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code
user_setup: []

must_haves:
  truths:
    - "`spec_fixtureRejection` returns envelope tag 0x02 with a guard id, zero domain bytes"
    - "`spec_fixtureTransportFault` returns envelope tag 0x03 — NOT JSON-RPC error"
    - "Solidity CI asserts both fixtures without string-matching revert text"
    - "Fixture methods ignore `--mode` (pure function of method name)"
  artifacts:
    - path: "solidity/test/SpecFixtures.t.sol"
      provides: "ROADMAP criterion 2 Solidity evidence"
      contains: "spec_fixtureRejection"
    - path: "scripts/fixtures-test.sh"
      provides: "CI-style gate for fixture tests"
      contains: "SpecFixtures"
  key_links:
    - from: "components/registry/src/Bridge/Registry.hs"
      to: "Bridge.Protocol GuardId / FaultCode"
      via: "SpecRejection / SpecTransportFault"
      pattern: "spec_fixture"
---

<objective>
Ship the two outcome fixture methods and prove them from Solidity — rejection and in-envelope transport
fault with no domain payload.

Purpose: SRV-07. When a domain method fails later, these fixtures are the control: if fixtures pass
and domain fails, the bug is in domain code — not the wire path.
</objective>

<context>
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-RESEARCH.md
@solidity/test/Discrimination.t.sol
@solidity/src/SpecOracle.sol
@components/registry/src/Bridge/Registry.hs

<verified_values>
**Fixed values (document in Registry haddock):**
- `spec_fixtureRejection` → `SpecRejection GuardStrikeOutOfRange` (guard id 3, matches Discrimination test)
- `spec_fixtureTransportFault` → `SpecTransportFault faultInternalError` (FaultCode 5)

**Mode independence:** `dispatch _ req` for fixture methods must not consult `LegacyMode`.
</verified_values>
</context>

<tasks>

<task type="auto" id="04-02-T1">
  <name>Add fixture handlers to Registry</name>
  <read_first>
    - components/registry/src/Bridge/Registry.hs
    - components/protocol/src/Bridge/Protocol.hs
  </read_first>
  <action>
    Extend dispatch:
    - `"spec_fixtureRejection"` → `mustOutcome id (SpecRejection GuardStrikeOutOfRange)`
    - `"spec_fixtureTransportFault"` → `mustOutcome id (SpecTransportFault faultInternalError)`
    Both ignore params (empty array `[]` accepted).
    Add registry unit tests mirroring Discrimination: encoded result contains no `"error"` key.
  </action>
  <verify>
    stack test evm-spec-bridge-registry --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - Both method names appear in Registry.hs
    - Tests assert tag paths via envelope decode or hex prefix known from Phase 3
  </acceptance_criteria>
</task>

<task type="auto" id="04-02-T2">
  <name>Solidity SpecFixtures.t.sol</name>
  <read_first>
    - solidity/test/Discrimination.t.sol
    - solidity/src/SpecOracle.sol
  </read_first>
  <action>
    Create `solidity/test/SpecFixtures.t.sol`:
    - `test_fixtureRejection_tagAndGuard`: call `spec_fixtureRejection`, decode envelope, assert
      `tag == TAG_REJECTION`, guard id 3
    - `test_fixtureTransportFault_tag`: call `spec_fixtureTransportFault`, assert `tag == TAG_FAULT`,
      version == PROTOCOL_VERSION
    Reuse `callOracle` / `decodeEnvelope` — no new call-site abstraction.
  </action>
  <verify>
    ./scripts/foundry-pin.sh && scripts/run-with-stub.sh success 8899 \
      "forge test --match-path test/SpecFixtures.t.sol -vvvv" exits 0
  </verify>
  <acceptance_criteria>
    - Two tests, both PASS with stub in success mode (fixtures mode-independent)
  </acceptance_criteria>
</task>

<task type="auto" id="04-02-T3">
  <name>just fixtures script</name>
  <read_first>
    - scripts/boundary-sweep.sh (pattern)
    - justfile
  </read_first>
  <action>
    Add `scripts/fixtures-test.sh`: foundry-pin, run-with-stub success 8899, match SpecFixtures.t.sol.
    Add `just fixtures` recipe calling the script.
  </action>
  <verify>
    just fixtures exits 0
  </verify>
  <acceptance_criteria>
    - Single command runs Solidity fixture suite
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `spec_fixtureRejection`, `spec_fixtureTransportFault` handlers
- `solidity/test/SpecFixtures.t.sol`
- `just fixtures` / `scripts/fixtures-test.sh`

<done>
Commit: `feat(04-02): fixture methods and Solidity CI`
</done>
