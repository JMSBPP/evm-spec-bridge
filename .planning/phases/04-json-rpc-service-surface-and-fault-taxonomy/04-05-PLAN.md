---
phase: 04-json-rpc-service-surface-and-fault-taxonomy
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - components/registry/src/Bridge/Registry.hs
  - components/transport/src/Bridge/Transport.hs
  - components/transport/test/Main.hs
  - components/registry/test/Main.hs
autonomous: false
parallelization: false
requirements: [PROTO-10]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code
user_setup: []

must_haves:
  truths:
    - "Fixture methods (health, fixtures) do not consult LegacyMode or any IORef"
    - "Same request bytes → same response bytes, tested not assumed"
    - "Module haddock documents the purity invariant explicitly"
    - "No running counter or request-id memory in Phase 4 server"
  artifacts:
    - path: "components/registry/src/Bridge/Registry.hs"
      provides: "Purity documented + mode-free fixtures"
      contains: "INVARIANT"
    - path: "components/transport/test/Main.hs"
      provides: "Repeat-request byte equality test"
      contains: "sameRequest"
  key_links:
    - from: "components/registry/src/Bridge/Registry.hs"
      to: "LegacyMode"
      via: "only spec_probe/spec_boundary/wedge paths"
      pattern: "LegacyMode"
---

<objective>
Prove handlers are pure — response is a function of request alone, with no cross-request state.

Purpose: PROTO-10. Phase 5's warm-process hardening assumes this invariant; if Phase 4 accidentally
introduces mutable state, fuzz/soak bugs become untraceable.
</objective>

<context>
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-RESEARCH.md
@components/registry/src/Bridge/Registry.hs

<verified_values>
**Scope:** purity applies to `spec_health`, `spec_fixtureRejection`, `spec_fixtureTransportFault`.
Legacy `spec_probe` may still use `LegacyMode` — document that exception.

**Test approach:** pure function `dispatch mode req` called twice with identical `req`; compare
`encode (dispatch mode req) == encode (dispatch mode req)`. Optional QuickCheck with arbitrary valid
requests for fixture methods only.
</verified_values>
</context>

<tasks>

<task type="auto" id="04-05-T1">
  <name>Audit and document purity invariant</name>
  <read_first>
    - components/registry/src/Bridge/Registry.hs
    - components/transport/src/Bridge/Transport.hs
  </read_first>
  <action>
    - Add module haddock INVARIANT block: fixture handlers are pure; LegacyMode affects only
      spec_probe/spec_boundary/wedge
    - Grep registry+transport for IORef, MVar, TVar, STM, global IO — remove if any added
    - Ensure dispatch signature stays pure: `LegacyMode -> RpcRequest -> Response`
  </action>
  <verify>
    rg 'IORef|MVar|TVar|STM' components/registry components/transport returns no matches in src/
  </verify>
  <acceptance_criteria>
    - Haddock INVARIANT visible in Registry module
  </acceptance_criteria>
</task>

<task type="auto" id="04-05-T2">
  <name>Repeat-request property tests</name>
  <read_first>
    - components/registry/test/Main.hs
    - components/jsonrpc/test/Main.hs (encodeResp helper)
  </read_first>
  <action>
    Add tests `sameRequestSameBytes_health`, `sameRequestSameBytes_fixtureRejection`,
    `sameRequestSameBytes_fixtureTransportFault`:
    - Build identical RpcRequest values
    - `let r1 = dispatch Success req; r2 = dispatch Success req`
    - assert encode r1 == encode r2
    Add transport-level test: two synthetic POST bodies through parse+dispatch path if exported for test.
  </action>
  <verify>
    stack test evm-spec-bridge-registry evm-spec-bridge-transport --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - At least 3 repeat-request tests for fixture methods
  </acceptance_criteria>
</task>

<task type="auto" id="04-05-T3">
  <name>Regression gate</name>
  <read_first>
    - justfile
  </read_first>
  <action>
    Run full local gate: just (or default target), just fixtures, just discriminate, just boundary-sweep.
    Record wall time in plan commit message body if notable.
  </action>
  <verify>
    just && just fixtures && just discriminate exits 0
  </verify>
  <acceptance_criteria>
    - No regression in Phase 3 or 04-02 fixtures
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- Documented PROTO-10 invariant in Registry haddock
- Repeat-request tests in registry/transport test suites

<done>
Commit: `test(04-05): handler purity invariant`
</done>
