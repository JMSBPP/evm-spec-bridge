---
phase: 04-json-rpc-service-surface-and-fault-taxonomy
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/jsonrpc/src/Bridge/JsonRpc.hs
  - components/jsonrpc/test/Main.hs
  - components/transport/src/Bridge/Transport.hs
  - components/registry/src/Bridge/Registry.hs
autonomous: false
parallelization: true
requirements: [PROTO-05, PROTO-08]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code
user_setup: []

must_haves:
  truths:
    - "Unknown JSON field on request object → BadParams or ParseFailure, never ignored"
    - "Out-of-range param values → BadParams with -32602, not silent clamp"
    - "Fault assertions use numeric `code` in emitted JSON, not message substring"
    - "No `parseId` fallthrough that maps garbage to IdInt 0"
  artifacts:
    - path: "components/transport/src/Bridge/Transport.hs"
      provides: "Strict request decode"
      contains: "parseRequest"
    - path: "components/jsonrpc/test/Main.hs"
      provides: "strict decode + fault code tests"
      contains: "-32602"
  key_links:
    - from: "components/jsonrpc/src/Bridge/JsonRpc.hs"
      to: "Network.JSONRPC errorParams"
      via: "protocolFault BadParams"
      pattern: "BadParams"
---

<objective>
Strict decoding and stable fault codes on the wire — unknown fields and bad params become typed
protocol faults, never silent defaults.

Purpose: PROTO-05 closes the "accept anything" hole in the Phase 3 stub parser. PROTO-08 ensures
consumers can branch on `code`, not English `message` text.
</objective>

<context>
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-RESEARCH.md
@components/jsonrpc/src/Bridge/JsonRpc.hs
@components/jsonrpc/test/Main.hs
@components/oracle-stub/app/Main.hs (old RpcRequest FromJSON — permissive)

<verified_values>
**Strict request shape:** object with exactly keys `jsonrpc`, `method`, `params`, `id` (allow `jsonrpc`
"2.0" only). Extra keys → `BadParams "unknown field"`.

**Params for fixture methods:** must be JSON array (empty ok). Object params → BadParams.

**Id parsing:** Number → IdInt; String → IdTxt; Null → reject with BadParams (not notification support).

**PROTO-08:** extend `protocolFaultCodes` test with at least one strict-decode case asserting `-32602`
and that `message` is NOT the sole test anchor (code is).
</verified_values>
</context>

<tasks>

<task type="auto" id="04-03-T1">
  <name>Strict request parser in transport</name>
  <read_first>
    - components/transport/src/Bridge/Transport.hs
    - components/jsonrpc/src/Bridge/JsonRpc.hs
  </read_first>
  <action>
    Add `parseRequest :: LBS.ByteString -> Either ProtocolFault (RpcRequest, Id)`:
    - Invalid JSON → ParseFailure
    - Top-level array → BadParams "batch not supported" (PROTO-09 overlap ok here)
    - Unknown fields → BadParams
    - Invalid jsonrpc version → BadParams
    - Null id → BadParams "notifications not supported"
    Wire into application: Left fault → protocolFault (default IdInt 0 only when parse failed before id read)
    Export RpcRequest type from transport or a small Bridge.Request module if cleaner.
  </action>
  <verify>
    stack build evm-spec-bridge-transport --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - Extra field `"foo":1` in request → error response
  </acceptance_criteria>
</task>

<task type="auto" id="04-03-T2">
  <name>Param schema enforcement for methods with params</name>
  <read_first>
    - components/registry/src/Bridge/Registry.hs (spec_boundary parseBoundaryIndex)
  </read_first>
  <action>
    Centralize params check: methods needing array params validate before dispatch.
    - `spec_boundary`: retain index 0..4 rule; out-of-range → BadParams
    - Methods with no params: reject non-empty array? **Decision:** accept `[]` only; `[null]` etc → BadParams
    Document in Registry which methods take params.
  </action>
  <verify>
    stack test evm-spec-bridge-registry --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - spec_boundary index 99 → fault not success
  </acceptance_criteria>
</task>

<task type="auto" id="04-03-T3">
  <name>JSON-RPC fault code tests</name>
  <read_first>
    - components/jsonrpc/test/Main.hs
  </read_first>
  <action>
    Add test group `strict decode`:
    - unknown field → encode contains -32602
    - invalid JSON → -32700
    Add test: BadParams response has numeric code field parseable from JSON (not only string grep).
    Optionally add `Bridge.JsonRpc.faultCode` helper exposing expected code per ProtocolFault for tests.
  </action>
  <verify>
    stack test evm-spec-bridge-jsonrpc --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - At least 3 new test cases in jsonrpc test suite
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `parseRequest` strict decoder
- Extended `jsonrpc/test` strict decode group
- Param validation for `spec_boundary`

<done>
Commit: `feat(04-03): strict decode and stable fault codes`
Run Phase 3 regression after merge.
</done>
