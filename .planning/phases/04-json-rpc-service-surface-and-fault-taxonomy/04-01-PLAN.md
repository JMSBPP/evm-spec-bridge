---
phase: 04-json-rpc-service-surface-and-fault-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/registry/package.yaml
  - components/registry/evm-spec-bridge-registry.cabal
  - components/registry/src/Bridge/Registry.hs
  - components/registry/test/Main.hs
  - components/transport/package.yaml
  - components/transport/evm-spec-bridge-transport.cabal
  - components/transport/src/Bridge/Transport.hs
  - components/oracle-stub/package.yaml
  - components/oracle-stub/app/Main.hs
  - stack.yaml
  - stack-core.yaml
  - justfile
autonomous: false
parallelization: false
requirements: [SRV-06]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code
user_setup: []

must_haves:
  truths:
    - "`spec_health` returns a hex envelope in `result`, not JSON-RPC `error`"
    - "Health success body is non-empty (domain-shaped bytes), not a ping string"
    - "Dispatch lives in `registry`; HTTP in `transport`; `Main.hs` is wiring only"
    - "Phase 3 recipes (`just discriminate`, `boundary-sweep`, `wedge-red`) still exit 0"
    - "The user can explain why a socket-open check is insufficient vs envelope-shaped health"
  artifacts:
    - path: "components/registry/src/Bridge/Registry.hs"
      provides: "Closed method dispatch including spec_health"
      contains: "spec_health"
    - path: "components/transport/src/Bridge/Transport.hs"
      provides: "warp Application factory"
      contains: "serve"
    - path: "components/registry/test/Main.hs"
      provides: "Unit test: spec_health encodes tag 0x01"
      contains: "spec_health"
  key_links:
    - from: "components/oracle-stub/app/Main.hs"
      to: "Bridge.Transport.serve"
      via: "imports registry handler"
      pattern: "Transport"
---

<objective>
**Tracer plan.** Stand up real `registry` + `transport` components and ship `spec_health` end-to-end —
the same tagged hex envelope a domain method will use.

Purpose: SRV-06. A green health check must prove domain payloads survive JSON-RPC + envelope encode,
not merely that TCP accepts connections. This plan is the vertical slice everything else hangs on.
</objective>

<context>
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-CONTEXT.md
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-RESEARCH.md
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-VALIDATION.md
@components/jsonrpc/src/Bridge/JsonRpc.hs
@components/oracle-stub/app/Main.hs
@components/protocol/src/Bridge/Protocol.hs

<verified_values>
**Health payload:** 32-byte sentinel `0xcd` repeated (matches Phase 3 boundary class 4 / success probe).
Encoded via `outcomeResponse` → envelope tag `TAG_SUCCESS = 0x01`, `protocolVersion = 1`.

**Legacy preservation:** `spec_probe`, `spec_boundary`, `--mode`, and wedge handler move to registry/transport
unchanged in behaviour — only location changes.

**Dependencies:** `registry` depends on `jsonrpc`, `protocol`, `abi-codec`. `transport` depends on
`registry`, `jsonrpc`, `wai`, `warp`, `aeson`. Add to `stack.yaml` / `stack-core.yaml`; run seam guard.
</verified_values>
</context>

<tasks>

<task type="auto" id="04-01-T1">
  <name>Implement Bridge.Registry dispatch with spec_health</name>
  <read_first>
    - components/registry/src/Bridge/Registry.hs
    - components/oracle-stub/app/Main.hs (handleRpc, serveProbe, serveBoundary)
    - components/jsonrpc/src/Bridge/JsonRpc.hs
    - components/protocol/src/Bridge/Protocol.hs
  </read_first>
  <action>
    Replace registry placeholder with:
    - `data LegacyMode = Success | Rejection | Fault | Boundary | Wedge` (or reuse from stub via export)
    - `dispatch :: LegacyMode -> RpcRequest -> Response` where `RpcRequest` has `method`, `id`, `params`
    - Handler `spec_health`: ignore params; `outcomeResponse id (SpecSuccess healthBody)` where
      `healthBody = BS.replicate 32 0xcd`
    - Move `spec_probe`, `spec_boundary`, `mustOutcome`, `boundaryBody` logic from Main.hs unchanged
    - Unknown methods for now: `protocolFault id (UnknownMethod method)` ( tightened in 04-04)
    Export: `dispatch`, `healthBody`, `LegacyMode`.
  </action>
  <verify>
    stack build evm-spec-bridge-registry --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - `spec_health` mentioned in Registry.hs
    - `SpecSuccess` path used for health (not raw JSON string)
  </acceptance_criteria>
</task>

<task type="auto" id="04-01-T2">
  <name>Implement Bridge.Transport HTTP layer</name>
  <read_first>
    - components/transport/src/Bridge/Transport.hs
    - components/oracle-stub/app/Main.hs (app, jsonResp, wedgeHandler)
  </read_first>
  <action>
    Add `serve :: LegacyMode -> Int -> IO ()` and internal `application :: LegacyMode -> Application`.
    - POST-only JSON body read (reuse strictRequestBody pattern)
    - Wedge mode: same forever-delay behaviour as today
    - Normal: decode minimal request → call `Registry.dispatch`
    - Response: JSON encode with Content-Type application/json
    Move `jsonResp`, `parseId` (temporary — strict decode in 04-03) into transport or shared module.
  </action>
  <verify>
    stack build evm-spec-bridge-transport --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - Transport module exports `serve`
    - No SpecOutcome construction in Transport (dispatch only)
  </acceptance_criteria>
</task>

<task type="auto" id="04-01-T3">
  <name>Thin oracle-stub + tests + regression</name>
  <read_first>
    - components/oracle-stub/app/Main.hs
    - components/jsonrpc/test/Main.hs (envelope assertion patterns)
    - justfile
    - scripts/run-with-stub.sh
  </read_first>
  <action>
    - Main.hs: parse `--port` and `--mode` only; call `Transport.serve mode port`
    - Add `components/registry/test/Main.hs`: POST handler unit test OR pure dispatch test —
      `dispatch Success req{method=spec_health}` yields Response whose encoded JSON contains `"result"`
      and `"0x"` prefix; decode envelope tag byte 0x01 (use abi-codec decode helper or hex slice)
    - Add `just health`: run-with-stub success 8899 curl POST spec_health, grep result 0x
    - Run: stack build --test --pedantic; just discriminate; just boundary-sweep; just wedge-red
  </action>
  <verify>
    stack build --test --pedantic exits 0 AND just discriminate exits 0
  </verify>
  <acceptance_criteria>
    - Main.hs under 80 lines of logic (wiring + opts)
    - Registry test passes
    - Phase 3 regression recipes exit 0
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- `Bridge.Registry.dispatch` — method table including `spec_health`
- `Bridge.Transport.serve` — loopback HTTP server
- `just health` — smoke recipe
- `LegacyMode` — retained for Phase 3 dev methods

<done>
Commit: `feat(04-01): registry transport tracer spec_health`
Update 04-01-SUMMARY stub if created inline; else defer to 04-06.
</done>
