---
phase: 04-json-rpc-service-surface-and-fault-taxonomy
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - components/registry/src/Bridge/Registry.hs
  - components/transport/src/Bridge/Transport.hs
  - components/transport/test/Main.hs
  - components/jsonrpc/test/Main.hs
autonomous: false
parallelization: false
requirements: [PROTO-06, PROTO-09]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code
user_setup: []

must_haves:
  truths:
    - "`eth_getBalance` (or any non-spec_*) returns -32601"
    - "All registered methods start with `spec_`"
    - "Request id echoed on success AND error responses (string and integer ids)"
    - "Batch JSON array at top level rejected"
    - "Notification (missing id or null id) rejected — not answered with result"
  artifacts:
    - path: "components/registry/src/Bridge/Registry.hs"
      provides: "spec_* namespace guard"
      contains: "spec_"
    - path: "components/transport/test/Main.hs"
      provides: "id echo and batch tests"
      contains: "batch"
  key_links:
    - from: "components/registry/src/Bridge/Registry.hs"
      to: "UnknownMethod"
      via: "non-spec prefix check before dispatch"
      pattern: "UnknownMethod"
---

<objective>
Namespace enforcement and request-shape discipline — only `spec_*` methods execute; everything else
is `-32601`; ids echo; batches and notifications are rejected.

Purpose: PROTO-06 prevents accidental domain RPC collision. PROTO-09 prevents silent mis-correlation
when Foundry reuses connections or when clients send malformed shapes.
</objective>

<context>
@.planning/phases/04-json-rpc-service-surface-and-fault-taxonomy/04-RESEARCH.md
@components/jsonrpc/src/Bridge/JsonRpc.hs
@.planning/phases/02-*/02-01-PROBE-NOTES.md (alloy sends id:0)

<verified_values>
**Namespace rule:** method must have prefix `spec_` (Text prefix check). `spec_health` ok; `eth_call` →
UnknownMethod → -32601.

**Registered methods (closed list):** spec_health, spec_fixtureRejection, spec_fixtureTransportFault,
spec_probe, spec_boundary. No dynamic registration in Phase 4.

**Id echo:** alloy uses `"id":0`; also test `"id":"abc"` string form.

**Batch:** if 04-03 already rejects top-level array, add explicit test here; else implement in transport.
</verified_values>
</context>

<tasks>

<task type="auto" id="04-04-T1">
  <name>spec_* namespace guard</name>
  <read_first>
    - components/registry/src/Bridge/Registry.hs
    - components/jsonrpc/src/Bridge/JsonRpc.hs
  </read_first>
  <action>
    In dispatch, before method lookup:
    - if method does not start with `spec_` → `protocolFault id (UnknownMethod method)`
    Add registry test: `eth_getBalance` encodes to -32601.
    Add test: `spec_health` still works.
  </action>
  <verify>
    stack test evm-spec-bridge-registry --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - Non-spec method never reaches outcomeResponse
  </acceptance_criteria>
</task>

<task type="auto" id="04-04-T2">
  <name>Id echo on all paths</name>
  <read_first>
    - components/jsonrpc/test/Main.hs (idEchoed)
    - components/transport/src/Bridge/Transport.hs
  </read_first>
  <action>
    Extend idEchoed pattern:
    - success spec_health with IdTxt "trace-1"
    - error unknown method with same id
    - BadParams path with IdInt 42
    Create `components/transport/test/Main.hs` with integration-style tests calling application function
    with mock Request if needed, OR pure encode after dispatch.
  </action>
  <verify>
    stack build --test --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - At least 3 id echo cases including error path
  </acceptance_criteria>
</task>

<task type="auto" id="04-04-T3">
  <name>Batch and notification rejection</name>
  <read_first>
    - components/transport/src/Bridge/Transport.hs (parseRequest)
  </read_first>
  <action>
    Ensure parseRequest rejects:
    - Top-level JSON array (batch)
    - Object with `"id": null`
    - Object missing `id` key entirely
    Tests in transport/test: each case → error response, no `result` key.
  </action>
  <verify>
    stack test evm-spec-bridge-transport --pedantic exits 0
  </verify>
  <acceptance_criteria>
    - Batch input returns fault, not array of responses
  </acceptance_criteria>
</task>

</tasks>

## Artifacts this phase produces

- Namespace guard in Registry
- `components/transport/test/Main.hs`
- Extended id echo coverage

<done>
Commit: `feat(04-04): spec_* namespace and request shape discipline`
</done>
