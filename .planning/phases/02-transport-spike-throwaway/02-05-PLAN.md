---
phase: 02-transport-spike-throwaway
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - .planning/phases/02-transport-spike-throwaway/02-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - justfile
  - spike/
autonomous: false
parallelization: false
requirements: [DIST-06]
execution_model: inline-with-user
background_agents: on-approval-mechanical-incl-code  # code chunks OK; checkpoints stay inline; see 02-CONTEXT
user_setup: []

must_haves:
  truths:
    - "The spike is gone and `rm -rf spike/` was genuinely the whole deletion"
    - "Every measurement survives the deletion, in PROBE-NOTES and 02-SUMMARY"
    - "The Foundry pin survives, and CI is green after the spike is removed"
    - "ROADMAP, REQUIREMENTS and STATE are updated IN THIS PLAN, not left for later"
    - "The user can state what Phase 2 proved and, specifically, what it did not"
  artifacts:
    - path: ".planning/phases/02-transport-spike-throwaway/02-SUMMARY.md"
      provides: "The durable record — the spike's only lasting output besides the pin"
      contains: "Content-Type"
    - path: ".planning/ROADMAP.md"
      provides: "Ledger showing Phase 2 complete with a real date"
      contains: "2. Transport Spike (Throwaway) | 5/5"
  key_links:
    - from: ".planning/REQUIREMENTS.md DIST-06 row"
      to: ".github/foundry-version"
      via: "traceability entry naming the surviving artifact"
      pattern: "DIST-06"
---

<objective>
Close the phase: consolidate the findings, delete the spike, and update the ledger — in this plan,
not afterwards.

Purpose: this plan exists in this shape because Phase 1's ledger was never updated. ROADMAP read
`0/9` and STATE said "Ready to plan" while nine plans sat committed, and it had to be patched by
hand in `41eb40c`. Under the Phase 2 mandate the executing work owns the ledger update, so it is a
task with acceptance criteria rather than a step someone remembers.

Output: `02-SUMMARY.md`, an empty `spike/`, and a truthful ledger.
</objective>

<context>
@.planning/phases/02-transport-spike-throwaway/02-CONTEXT.md
@.planning/phases/02-transport-spike-throwaway/02-VALIDATION.md
@.planning/phases/02-transport-spike-throwaway/02-01-PROBE-NOTES.md
@.planning/phases/01-repo-skeleton-component-seams-ci-floor/01-SUMMARY.md
@.planning/ROADMAP.md

<verified_values>
Phase 1's closing pattern, to follow: `01-SUMMARY.md` records goal-vs-outcome, a success-criteria
verdict table, measured values with their conditions, decisions governing later phases, corrections
made, and what carries forward.

ROADMAP rows to update:
  `| 2. Transport Spike (Throwaway) | 0/TBD | In progress | - |`
STATE fields to update: Current Position, Performance Metrics, Blockers/Concerns, Session Continuity.
REQUIREMENTS: the DIST-06 checkbox and its traceability row.

DIST-06 as written: "This repo pins an exact Foundry version, published as part of the integration
contract, so every measured transport finding is scoped to something enforceable."
Note "published as part of the integration contract" — assess honestly whether that half is met by
a file in our repo, or whether it completes only at Phase 10.
</verified_values>
</context>

<tasks>

<task type="auto" id="02-05-T1">
  <name>Task 1: Write 02-SUMMARY.md — the findings must outlive the code</name>
  <read_first>
    - .planning/phases/02-transport-spike-throwaway/02-01-PROBE-NOTES.md (every measurement taken this phase)
    - .planning/phases/01-repo-skeleton-component-seams-ci-floor/01-SUMMARY.md (the format to follow)
  </read_first>
  <teaching>
    The spike is deleted in the next task. Everything of value must already be in this document
    before that happens — write it FIRST, delete SECOND. Reversing the order means writing the
    summary from memory of code you can no longer read.

    Every number carries its conditions. Phase 1's seam guard was measured at 0.34 s warm, quoted
    unqualified, and was 118 818 ms cold — a 350x error that changed the CI design. State the forge
    version and commit, the solc version, the OS, and that everything is loopback and single-host.
  </teaching>
  <action>
    Write `.planning/phases/02-transport-spike-throwaway/02-SUMMARY.md` with frontmatter matching
    01-SUMMARY.md's shape (phase, status, completed, plans, requirements) and these sections:

      - Phase goal vs outcome
      - Success-criteria verdict table against the AMENDED criteria 1-5 in ROADMAP
      - **The Content-Type matrix**, reproduced in full with its conditions, and an explicit
        statement of whether ARCHITECTURE.md:612 is retired
      - **The return-path shape**, stated in prose, with whether it agreed with PITFALLS.md:103
      - **The hex-envelope result**, with whether it agreed with PITFALLS.md:82
      - What was consumed rather than re-derived, and from where (the try/catch and rpcJson
        measurements)
      - Measured values with conditions: build times, `forge test` duration, anything timed
      - Corrections made to this session's own claims, if any
      - What carries forward to Phase 3 — especially anything the decode path depends on
      - **What this phase did NOT prove**: single host, single OS, one observation each, nothing
        about load, concurrency, repetition, proxies, or the consumer's runner

    If any finding contradicted research, give it its own clearly-titled section rather than
    folding it into prose.
  </action>
  <verify>
    test -f .planning/phases/02-transport-spike-throwaway/02-SUMMARY.md
    grep -q 'Content-Type' .planning/phases/02-transport-spike-throwaway/02-SUMMARY.md
  </verify>
  <acceptance_criteria>
    - `02-SUMMARY.md` exists with valid frontmatter
    - It contains the full Content-Type matrix with its conditions
    - It states explicitly whether ARCHITECTURE.md:612 is retired
    - It states whether the return-path shape agreed with PITFALLS.md:103
    - It states whether the hex envelope agreed with PITFALLS.md:82
    - It contains a section naming what the phase did NOT prove
    - Every timing quoted carries its conditions (warm/cold, host, versions)
  </acceptance_criteria>
  <done>02-SUMMARY.md records every measurement with its conditions, including what the phase did NOT prove — written BEFORE the spike is deleted.</done>
</task>

<task type="checkpoint:human-verify" id="02-05-T2" gate="blocking">
  <name>Task 2: Delete the spike — and prove the deletion claim was true</name>
  <read_first>
    - .planning/phases/02-transport-spike-throwaway/02-02-PLAN.md (task 02-02-T1, where "deletion is `rm -rf spike/`" was claimed)
    - justfile (the three spike recipes)
  </read_first>
  <teaching>
    02-02-T1 claimed the isolated-project layout would make deletion `rm -rf spike/` and nothing
    else. This task is where that claim gets tested. If deletion turns out to require touching
    `stack.yaml`, regenerating a `.cabal`, or re-running the seam guard, then the isolation was
    incomplete and that is a finding worth recording — a small one, but the same shape as every
    other "the check could not see what it was pointed at" in this project.

    Delete with `git rm -r` rather than `rm -rf`, so the removal is a reviewable diff rather than a
    disappearance.
  </teaching>
  <action>
    1. BEFORE deleting, record the current state: `git status --porcelain` (clean), and confirm
       `02-SUMMARY.md` is committed.
    2. `git rm -r spike/`
    3. Remove the `spike-server`, `spike-test` and `spike-matrix` recipes from `justfile`, and the
       comments referring to them.
    4. Verify the claim: `git status --porcelain` should show ONLY `spike/` deletions and the
       `justfile` edit. If anything else appears — a stack.yaml change, a .cabal regeneration — the
       isolation claim was wrong. Record that in 02-SUMMARY.md.
    5. Confirm the root project is unaffected:
         - `stack build` exits 0
         - `./scripts/seam-guard.sh` exits 0
         - `./scripts/hpack-drift.sh` exits 0
         - `./scripts/foundry-pin.sh` exits 0  ← the pin must SURVIVE
    6. Confirm no residue: `grep -rn "spike" --include='*.yaml' --include='*.yml' --include='justfile' . | grep -v '.planning'` returns nothing.
    7. Commit, push, and confirm the CI run is green with the spike gone.
  </action>
  <verify>
    git rm -r spike/
    git status --porcelain
    test ! -d spike
    stack build; echo "stack exit=$?"
    ./scripts/seam-guard.sh; echo "seam exit=$?"
    ./scripts/hpack-drift.sh; echo "drift exit=$?"
    ./scripts/foundry-pin.sh; echo "PIN SURVIVES exit=$?"
    grep -rn 'spike' --include='*.yaml' --include='*.yml' --include='justfile' . | grep -v '.planning'
  </verify>
  <acceptance_criteria>
    - `spike/` does not exist: `test ! -d spike` succeeds
    - `git status --porcelain` after step 3 shows only spike deletions plus the justfile edit
    - `stack build` exits 0
    - `./scripts/seam-guard.sh` exits 0
    - `./scripts/hpack-drift.sh` exits 0
    - **`./scripts/foundry-pin.sh` exits 0** — the permanent artifact survived
    - `.github/foundry-version` still exists and still matches the consumer's values
    - The residue grep returns no matches outside `.planning/`
    - A green CI run after deletion is recorded with its run ID
    - 02-SUMMARY.md records whether the `rm -rf spike/` deletion claim held
  </acceptance_criteria>
  <done>The spike is deleted, the `rm -rf spike/` claim was tested, and the Foundry pin plus a green CI run survive it.</done>
</task>

<task type="auto" id="02-05-T3">
  <name>Task 3: Update the ledger — ROADMAP, REQUIREMENTS, STATE</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 block, the Progress table, and the plan checkbox list)
    - .planning/REQUIREMENTS.md (the DIST-06 checkbox and its traceability row)
    - .planning/STATE.md (Current Position, Performance Metrics, Blockers, Session Continuity)
  </read_first>
  <teaching>
    This is the task that exists because it was missed last time. Under the Phase 2 mandate the
    executing work owns the ledger, so it is here with acceptance criteria rather than left as
    something to remember afterwards.

    Assess DIST-06 HONESTLY. Its text says "pins an exact Foundry version, **published as part of
    the integration contract**". We have the pin. Whether it is *published as part of an integration
    contract* is arguably not complete until the consumer actually consumes it — which is Phase 10.
    If so, mark it Partial with the reason, exactly as DIST-04 was marked Partial in Phase 1 rather
    than rounded up to Met.
  </teaching>
  <action>
    In `.planning/ROADMAP.md`:
      - tick the Phase 2 checkbox in the Phases list
      - add the five plan filenames as ticked checkboxes under Phase 2's `**Plans**:` line, replacing `TBD`
      - update the Progress table row to `| 2. Transport Spike (Throwaway) | 5/5 | Complete | <date> |`
      - set Phase 3's row to `In progress`

    In `.planning/REQUIREMENTS.md`:
      - update the DIST-06 checkbox and its traceability row with an honest verdict (Met or
        Partial) and a one-line evidence pointer naming `.github/foundry-version` and the plan

    In `.planning/STATE.md`:
      - Current Position → Phase 3, status, last activity, progress bar
      - Performance Metrics → add a Phase 2 row and any measured timings
      - Blockers/Concerns → close what Phase 2 closed; carry what it did not; add anything new
      - Session Continuity → stopped-at and resume-file

    Commit all three together with the summary, in one commit, so the ledger cannot be half-updated.
  </action>
  <verify>
    grep -c '5/5 | Complete' .planning/ROADMAP.md
    grep -n 'DIST-06' .planning/REQUIREMENTS.md
    grep -n 'Phase: 3' .planning/STATE.md
    git log -1 --stat
  </verify>
  <acceptance_criteria>
    - `grep -c '5/5 | Complete' .planning/ROADMAP.md` returns at least 1
    - ROADMAP's Phase 2 `**Plans**:` line lists all five plan files, each ticked, with no `TBD`
    - The Phase 2 checkbox in the Phases list is `[x]`
    - REQUIREMENTS' DIST-06 traceability row no longer reads `Pending` and names the evidence
    - STATE's Current Position names Phase 3
    - STATE's Blockers section no longer lists any Phase 2 item as open unless it genuinely is
    - All ledger files are updated in a SINGLE commit together with 02-SUMMARY.md
  </acceptance_criteria>
  <done>ROADMAP, REQUIREMENTS and STATE are updated together in a single commit with 02-SUMMARY.md, and DIST-06 carries an honest verdict.</done>
</task>

<task type="checkpoint:human-verify" id="02-05-T4" gate="blocking">
  <name>Task 4: Close-out review with the user</name>
  <read_first>
    - .planning/phases/02-transport-spike-throwaway/02-SUMMARY.md
    - .planning/ROADMAP.md (Phase 3, to confirm what the findings feed)
  </read_first>
  <teaching>
    The user is the review — there is no plan-checker and no reviewer agents by their decision. So
    this task is the gate. The pedagogical mandate means the test is not "are the artifacts
    present" but "can the user explain them".
  </teaching>
  <action>
    Walk the user through:
      1. The Content-Type verdict and what it means for Phase 4's server.
      2. The return-path shape, and precisely which Solidity decode Phase 3 must now write.
      3. Whether anything contradicted research, and what was done about it.
      4. What Phase 2 did NOT prove — read that section aloud together.
      5. Confirm the ledger reads truthfully: ROADMAP 5/5, STATE at Phase 3, DIST-06's honest verdict.

    Ask the user directly whether anything in the summary overstates what was observed. Weaken any
    claim they challenge rather than defending it.
  </action>
  <verify>
    git status --porcelain
    git log --oneline -5
  </verify>
  <acceptance_criteria>
    - The user has confirmed the summary does not overstate the observations
    - The user can state what Phase 3's decode path must do, based on the recorded return shape
    - Any claim the user challenged has been weakened or evidenced
    - `git status --porcelain` is clean and everything is pushed
  </acceptance_criteria>
  <done>The user has confirmed the summary does not overstate the observations, and the tree is clean and pushed.</done>
</task>

</tasks>

<verification>
Phase 2 is complete when: `02-SUMMARY.md` records every measurement with its conditions; `spike/`
is deleted and the deletion claim from 02-02-T1 has been tested; `.github/foundry-version` and
`scripts/foundry-pin.sh` survive with CI green; ROADMAP shows 5/5 Complete; REQUIREMENTS gives
DIST-06 an honest verdict; STATE names Phase 3; and the user has confirmed nothing in the summary
overstates what was observed.

Do NOT auto-advance to Phase 3. Stop for the user.
</verification>
